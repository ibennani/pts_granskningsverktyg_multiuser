/**
 * @fileoverview Delade konstanter för modallayout och animation i ModalComponent.
 */

/** Maximal andel av viewport som modal får uppta i bredd och höjd. */
export const MODAL_MAX_VIEWPORT_RATIO = 0.9;

/** Opacity-animation vid öppning och stängning (millisekunder). */
export const MODAL_TRANSITION_MS = 500;
